[pytest]
testpaths = tests
python_files = test_*.py
python_classes = *Tests
python_functions = test_*
markers =
    live: tests that may require network access, provider credentials, or external services
addopts = -ra
